import { describe, expect, test } from 'vitest'
import { weatherCodeMap } from '@repo/shared/constants'

describe('weatherCodeMap', () => {
    
    test('todo', () => {
        
        expect(weatherCodeMap).toBeDefined()
        
    })
    
})

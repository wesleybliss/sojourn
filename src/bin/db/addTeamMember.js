import 'dotenv/config'
import db from '../../db2/index.js'
import * as schemas from '../../db2/schema.js'

const main = async () => {
    
    
    
}

main()
    .then(() => process.exit(0))
    .catch(e => {
        console.error(e)
        process.exit(1)
    })
